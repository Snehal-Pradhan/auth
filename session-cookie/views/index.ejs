<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Theme Controller</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Link to your external CSS file -->
    <link rel="stylesheet" href="/styles.css">
</head>
<!-- Dynamically apply theme class based on the 'currentTheme' variable passed from Express -->

<body class="<%= currentTheme === 'dark' ? 'theme-dark' : 'theme-light' %>">
    <div id="app" class="container max-w-lg w-full">
        <!-- Display theme message dynamically -->
        <h1 id="themeMessage" class="text-3xl font-bold mb-6">
            It is <%= currentTheme==='dark' ? 'Dark' : 'Light' %> Theme
        </h1>

        <div class="flex justify-center space-x-4">
            <!-- Buttons link to the /set-theme route with query parameters -->
            <a href="/set-theme?theme=dark"
                class="px-6 py-3 rounded-md bg-gray-700 text-white font-semibold shadow-md hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-opacity-75 transition duration-300 ease-in-out">
                Set Dark Theme
            </a>
            <a href="/set-theme?theme=light"
                class="px-6 py-3 rounded-md bg-blue-500 text-white font-semibold shadow-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75 transition duration-300 ease-in-out">
                Set Light Theme
            </a>
        </div>
    </div>
</body>

</html>